
<div class="regex_unitaire">
    <span class="regex_name">
        Expression régulière 1 : 
    </span>
    <input type="text" name="question_sarah_1_1" value="<?php echo $data; ?>"/>
    <FORM>
        <INPUT type= "radio" name="action" value="reponse"> SARAH répond à la question
        <INPUT type= "radio" name="action" value="fonction"> SARAH redirige vers une question
        <INPUT type= "radio" name="action" value="executable"> SARAH éxécute une commande
    </FORM>
    <br/>
    <div class="sup_regex">
        Supprimer un champ regex
    </div>
</div>
